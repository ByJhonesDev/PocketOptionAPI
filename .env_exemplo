# ============================================================================
# .env — ÚNICO (DEMO e REAL) — Pocket Option / WS / Análise 100 Candles/Velas
# Preencha os campos marcados por <>. Este arquivo único cobre:
# - Sessões DEMO e REAL (preencha as duas e escolha com PO_ACCOUNT_MODE)
# - Leitura de WebSocket/Socket.IO e HTTP
# - Filtros de Ativos, Payout Mínimo, Analise de 100 Candles/Velas
# - Overrides Opcionais (DNS/SNI/SSL) e Proxies
# ============================================================================

# =======================
# CREDENCIAIS / SESSÕES
# =======================
# Preencha os dois e selecione o modo em PO_ACCOUNT_MODE (PRACTICE|REAL)
POCKET_OPTION_SSID_REAL=<SEU_SSID_REAL>
POCKET_OPTION_SSID_DEMO=<SEU_SSID_DEMO>

# PRACTICE | REAL
PO_ACCOUNT_MODE=PRACTICE

# Linha de cookies Cloudflare (Preenchido pelo get_cf_cookies.py)
# Exemplo: cf_clearance=abc...; _cfuvid=xyz...
PO_EXTRA_COOKIES=cf_clearance=<SEU_CF_CLEARANCE>; _cfuvid=<SEU_CFUVID>

# ======================
# TIMEFRAMES E ANÁLISE
# ======================
# Timeframe alvo em minutos (Ex.: 1 = M1, 5 = M5).
PO_TIMEFRAMES=1,5
# Analise de Candles/Velas para Cálculo/Indicadores (Padrão: 100 Velas)
PO_PAST_CANDLES=100

# ================================
# ATIVOS (DINÂMICO) — SOMENTE OTC
# ================================
# Exibir apenas Ativos Abertos e com Payout Mínimo?
PO_ONLY_OPEN_AND_MIN_PAYOUT=true
# Payout Mínimo (%) para considerar na Lista (Ex.: 90 = 90%)
PO_MIN_PAYOUT_PERCENT=90
# Atualizar Lista/Estado de Ativos a cada N Segundos (Padrão: 5 Segundos)
PO_REFRESH_ASSETS_EVERY_SEC=5
# Filtrar por sufixo do símbolo (Ex.: _otc). Vazio = todos.
PO_SYMBOL_SUFFIX_FILTER=_otc
PO_ENABLE_HEURISTIC_OPEN_CHECK=true
# Considerar apenas pares OTC do tipo Forex?
PO_OTC_ONLY_FOREX=true

# ============================
# ENDPOINTS / WS
# ============================
# Base HTTP/WS da Corretora
PO_WS_HTTP_BASE=https://api-c.po.market
# Caminho do Socket.IO
PO_SOCKETIO_PATH=/socket.io
# Origin/Referer esperado pelo servidor WS
PO_WS_ORIGIN=https://pocketoption.com
# User-Agent a ser usado no WS/HTTP (Use seu UA Real, se necessário)
PO_WS_UA=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, como Gecko) Chrome/140.0.0.0 Safari/537.36
# Lista de candidatos de endpoint WS (separe por vírgula). Teste quais funcionam na sua rede.
PO_WS_CANDIDATES=wss://api-c.po.market/socket.io/?EIO=4&transport=websocket

# Limite máximo (Segundos) esperando frames úteis do WS antes de abortar
WAIT_WS_MAX=600

# ============================
# HTTP REST (opcional para Assets/Histórico)
# ============================
PO_HTTP_API_BASE=https://api.pocketoption.com
PO_HTTP_ASSETS_PATH=/api/v1/assets
PO_HTTP_HISTORY_PATH=/api/v1/history

# ============================
# SSL / Diagnóstico (use apenas para depurar; retorne para true)
# ============================
PO_VERIFY_SSL=false

# ============================
# Overrides opcionais de DNS/SNI (use só se precisar)
# ============================
# PO_WS_RESOLVE_IP força resolver o host para um IP específico (contorna DNS),
# e PO_WS_SNI_HOST define o host SNI no TLS (contorna certificados multi-host).
PO_WS_RESOLVE_IP=
PO_WS_SNI_HOST=

# ============================
# Navegador / WebDriver (para captura de SSID/cookies, se aplicável)
# ============================
BROWSER=chrome
HEADLESS=false
USER_AGENT=

# ============================
# Persistência / Logs
# ============================
OUT_DIR=GET_SSID
SAVE_RAW_WS=1
LOG_LEVEL=INFO

# ============================
# Caminhos de saída de SSID (seu fluxo pode ler daqui)
# ============================
SSID_DEMO_PATH=GET_SSID/SSID_DEMO.txt
SSID_REAL_PATH=GET_SSID/SSID_REAL.txt
